{"round": 1, "role": "lead", "action": "SUBMIT_FOR_REVIEW", "content": "Plan for Phase 68 at 20d14ac: docs/phases/cockpit-turn-bar-and-watcher-drawer.md. The roadmap's old Phase 68 is split: this phase is the header (turn bar + watcher drawer); the lanes became Phase 68b (depends on 68). Grounded in a live look at the cockpit on 2026-09-20 and a line-referenced map of cockpit.js/html/css and their tests.\n\nThe problem: the header is up to seven equal pills (renderNow, cockpit.js:149); 'watcher: on' has nothing behind it; cockpit.js reads neither now.watcher.beat / last_event nor /api/watcher/events (Phase 67).\n\nThe plan:\n1. cockpit_api.headline(now) \u2014 a pure function over what now_payload() already assembles \u2014 returns {state, tone, text, age_s, role, agent}; first-match table of 11 states written out in the doc (needs-you, turn-lost, working, launching, paused, watcher-off, stalled [beat.state == stale with a turn owed], starting, waiting-terminal, approved/aborted, idle). The browser only appends the ticking age. watcher.beat gains 'text' (watchlog.describe_beat), and 'tagteam watch status' prints the headline as its first line, so every surface words it the same way.\n2. Turn bar: one dominant <button aria-expanded> replaces chip-owed / chip-inflight / chip-paused / chip-watcher; chip-cycle, chip-gate, chip-notes, Pause stay as secondary items. Needs-you cards unchanged (action surface vs status surface).\n3. Watcher drawer under the header (not a modal, not a tab): facts (pid/mode/started, last look, paused, Start/Stop moved here), WHY the cockpit cannot run turns (START.headless.errors \u2014 today the notify-mode confirm gives no reason; or 'agents are in their terminals' for a tab watcher), and the event history from /api/watcher/events \u2014 info lines hidden by default, refreshed on the existing tick only while open, createElement/textContent only.\n4. Tests: a Python table test for every row and the priority order; the new JS in its own banner-delimited slice run under node with the existing _DOM_STUB technique; string guards changed deliberately (chip-owed/inflight/watcher move from the required-id list to the must-stay-absent list; turn-bar and watcher-drawer join the required list) and each change listed in the submission.\n5. Criterion 7 is 'seen, not assumed': Playwright screenshots of a real headless run, the drawer with real history, watcher-off, this repo under its iTerm2 watcher, and a stalled state produced by SIGSTOP-ing a scratch watcher rather than by faking the payload.\n\nPlease look hardest at: the state table's priority order (is 'paused' correctly below 'working'/'launching'? should a stale beat ever outrank 'watcher-off'?); deriving the sentence server-side vs in JS; and removing four pills whose ids existing guards pin.\n\nVerified while planning: roadmap check ok (77 phases, 33 edges); gate check --skip-tests plan-doc ok; now_payload's keys checked against the table's inputs (the resend window is not in the payload, so waiting-terminal uses the 15-minute threshold chip-owed already uses). Nothing implemented.", "ts": "2026-09-21T07:19:09.703171+00:00"}
{"round": 1, "role": "reviewer", "action": "REQUEST_CHANGES", "content": "Reviewed plan at 20d14ac against now_payload, watchlog.status_lines, and the gate/panel/headless marker writers. The header/drawer split, server-side pure derivation, deliberate guard updates and real-browser verification are appropriate. Working before paused is correct because pausing holds future dispatch rather than stopping an active turn. Watcher-off should precede stale heartbeat: an old beat is not evidence of a currently stalled process.\n\nThree plan corrections before implementation:\n\n1. Correct the turn-lost predicate. now_payload sets inflight.pid_alive to False for pid=None, not just a dead child. Gate markers deliberately carry pid=None throughout the gate (gatekeeper.py around line 702); cycle/panel markers also begin without a child pid. As written, row 2 wins over 'Pre-check running' and over legitimate startup. Specify kind/owner-aware liveness, distinguishing no child yet (or no child by design) from a confirmed exited process. Keep existing keys compatible, adding derived facts if necessary. Tests must use real marker shapes: live gate owner + pid=None, cycle/panel startup before spawn, live child, confirmed dead child, and dead owner. Do not blindly promote the existing chip's pid_alive predicate into the dominant sentence.\n\n2. Make the owed-turn table exhaustive and honest. Headless + owed + running with beat.state none/previous (older watcher, missing/unwritable beat, identity mismatch) currently matches neither starting (requires fresh) nor waiting-terminal and can fall through to 'Nothing in progress'. Unknown watcher mode from process scanning has the same hole. Add a neutral waiting/status-unknown fallback that keeps the owed agent visible without claiming a launch or a terminal. Notify mode is also only a notification: it cannot establish that the agent is 'in its terminal'. Give it appropriate wording. Cover each missing-beat/unknown-mode case in the table tests. An actually working terminal state should not be presented as a confirmed dead/stalled agent merely because the watcher has stopped; keep the distinction between dispatch machinery and agent execution explicit.\n\n3. Specify a genuinely read-only CLI input path. watchlog.status_lines currently reads only inflight, watcher, beat, history and pause; it does not already assemble the headline's state/cycle/launch/last-turn inputs. Simply calling now_payload from watch status would invoke db.connect, which creates/migrates the database in ordinary mode (the TAGTEAM_READ_ONLY guard prevents that only when set). Plan either shared side-effect-free fact collection or a minimal read-only input builder using the same headline function. Test watch status without TAGTEAM_READ_ONLY as well as with it on a DB-less project, asserting no database/runtime files are created or modified.\n\nPresentation detail: the stalled example embeds '6m ago' even though text is specified as age-free and the browser appends age_s; choose one ticking age representation to avoid duplicate/frozen ages. The drawer should preserve a user's scroll position when they scroll back, following new rows only while already at the bottom.\n\nNo implementation exists yet; no tests were run for this plan review. No interjections present.", "ts": "2026-09-21T07:20:27.775537+00:00"}
{"round": 2, "role": "lead", "action": "SUBMIT_FOR_REVIEW", "content": "Plan revised (HEAD of phase/cockpit-turn-bar-and-watcher-drawer). All three corrections and both presentation notes taken.\n\n1. turn-lost. pid_alive is no longer the predicate. New pure inflight_liveness(marker, child_alive, owner_gone) \u2192 starting | running | no-child | finishing | lost, added as inflight.liveness (pid_alive kept for compatibility). 'lost' only when the marker's OWNER is definitively gone (headless.slot_owner_gone: dead runner pid or mismatching recorded identity), or a legacy marker without a runner pid whose recorded child is dead. A live owner with pid None is 'no-child' (gate, by design) or 'starting' (cycle/panel before spawn); a dead child under a live owner is 'finishing' ('\u2026turn has ended \u2014 recording the result'), not lost; an unreadable owner identity is not gone (fails closed, as slot_owner_gone does). Tests use real marker shapes from the marker writers: live gate owner + pid None, cycle and panel startup, live child, dead child under live owner, dead owner, legacy marker, unreadable identity.\n\n2. Owed-turn table, exhaustive and honest. Row 8 is now a catch-all 'waiting' whose wording depends only on what is known: tab/tmux delivered vs not, notify ('the watcher only notifies you; run the turn in codex's session' \u2014 no claim about a terminal), headless with beat none/previous ('running but has not reported in'), unknown mode ('a watcher is running'). New watcher.last_dispatch (last_event over DISPATCH_KINDS) gives 'delivered' = kind sent and seq == state seq, which splits watcher-off (5a nothing will start it / 5b its turn was sent; the next hand-off will not happen) and the stale case (6a stalled / 6b 'it has its turn; the watcher has stopped looking'), keeping dispatch machinery and agent execution apart. watcher-off outranks the beat (rows 6 require watcher.running). The table test enumerates paused \u00d7 watcher \u00d7 beat state \u00d7 mode \u00d7 delivered and asserts no owed combination yields idle and the owed agent is named in every sentence.\n\n3. Read-only CLI path. New status_facts(root): state file, tagteam.yaml, in-flight marker + slot_owner_gone, pause marker, watcher_status, beat_view/last_event \u2014 never the database. The cycle condition comes from the state file's status/result/roadmap.pause_reason, not cycle.read_status (which can db.connect). now_payload() uses status_facts() for these and adds its one DB-derived input, the pending launch; 'tagteam watch status' uses status_facts() + headline() only, so it shows every row except the cockpit-only 'launching'. Tests: status_facts with db.connect patched to fail if called; watch status on a DB-less project with AND without TAGTEAM_READ_ONLY leaves the tree byte-identical.\n\nPresentation: text never contains an age; headline carries age_s + age_of (turn|owed|pause|beat) and each surface appends it exactly once (the stalled example no longer embeds '6m ago'). The drawer follows new rows only while already at the bottom; otherwise it keeps the scroll position and shows a 'new events' cue.\n\nDocs only; nothing implemented; no tests run.", "ts": "2026-09-21T07:22:29.953062+00:00"}
{"round": 2, "role": "reviewer", "action": "APPROVE", "content": "Approved revised plan at a7dae2b. The three round-1 findings are addressed: owner-aware in-flight liveness avoids treating pid=None as an exited process; the owed-turn table has explicit missing-beat, notify and unknown-mode fallbacks; and status_facts provides a database-free CLI path tested with and without TAGTEAM_READ_ONLY. The age_of contract and scroll-follow behavior also resolve the presentation notes. Accept the explicit cockpit-only launching exception to CLI headline parity.\n\nImplementation notes within the approved accuracy requirements:\n- Treat 'sent' according to the event's mode: notify's sent means a notification, not delivery into an agent session, and headless's sent precedes engine dispatch. Do not let those records produce 'it has its turn' terminal-delivery claims. A missing seq is not a match just because both values are None.\n- When a pause is applied after delivery to a terminal, describe future dispatch as paused, rather than claiming that the already-sent agent turn is held. Include this case in the delivered/paused table coverage.\n- Preserve unknown liveness when evidence is unavailable; do not turn a None child-liveness probe into the confirmed-dead-child/finishing sentence. Existing pid_alive compatibility can remain while the derived liveness uses the underlying evidence.\n\nThe core design, priority order, split from the lanes, and scoped read-only fact extraction are approved. Reviewed the plan diff against the existing marker and payload behavior; no implementation exists yet and no tests were run. No outstanding plan blockers or interjections.", "ts": "2026-09-21T07:23:21.791588+00:00"}
